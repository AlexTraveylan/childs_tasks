@docs/index.md

## Règles importantes

- Après avoir touché, créé ou modifié une fonctionnalité, mettre à jour le fichier `.md` correspondant dans `docs/`.
- Mot de passe parent : voir `docs/auth.md` — problème critique avec les `\$` dans `.env.local`.
